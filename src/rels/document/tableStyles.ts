import JSZip from "jszip";
import { SweepOptions } from "../..";

export async function modifyTableStyles(
  zip: JSZip,
  referencingRelsPath: string,
  documentPath: string,
  options: SweepOptions
): Promise<void> {
  // TODO: Implement the function
}
